package com.aws.sif.execution;

public enum OptionalParamKey {
    version,
    versionAsAt,
    tenant,
    group,
    defaultValue,
    ignoreCase,
    locale,
    timezone,
    roundDownTo,
	quantityKind,
	limit,
	index,
}
